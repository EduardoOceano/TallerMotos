@model IEnumerable<TallerMotos.Models.Productos>

@{
    ViewData["Title"] = "Index";

    Layout = "~/Views/Shared/_Layout2.cshtml";

}
<br />
<br />
<br />
<br />
<h1>Index</h1>

<p>
    <a asp-action="Create">Crear Producto</a>
</p>
<table class="table table-dark">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tipo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.precio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.stock)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.idProveedor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.fabricante)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td id="tipo">
                @Html.DisplayFor(modelItem => item.tipo)
            </td>
            <td id="precio">
                @Html.DisplayFor(modelItem => item.precio)
            </td>
            <td id="stock">
                @Html.DisplayFor(modelItem => item.stock)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.idProveedor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.fabricante)
            </td>
            <td>
                <a asp-action="Create" asp-route-id="@item.id">Create</a>|
                <a asp-action="Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id">Delete</a> |
                <a asp-action="ListadoProductos">Listado Productos</a> |
                <a asp-action="Index" asp-controller="ProductosProveedores">Listado Productos Proveedores</a> |
                <button class="btn-primary" onclick="anyadirProductosAlcarrito()">Comprar</button>

            </td>
        </tr>
}
    </tbody>
</table>
